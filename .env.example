# Optional: only needed if using API-key billing instead of a subscription.
# Most users should use `just login <name>` instead (OAuth).
# Any keys set here are forwarded to containers automatically.
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
# OPENAI_API_KEY=
